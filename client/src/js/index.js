import "./controllers/searchController";

const state = {
  test: "xd"
}

/* IDGAF */
window.addEventListener("click", searchController);

/* (async () => {
  const response = await fetch("/api/search?q=");
  const data = await response.json();
  alert(data[0]);
})(); */