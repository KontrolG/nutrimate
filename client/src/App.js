import React, { StrictMode } from "react";
import Router from "./components/Router";
import "./App.css";

const App = () => {
  return (
    <StrictMode>
      <Router />
    </StrictMode>
  );
};

export default App;
